% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dates.R
\name{analyze_dates}
\alias{analyze_dates}
\title{Profile date / datetime columns}
\usage{
analyze_dates(df, types = NULL)
}
\arguments{
\item{df}{A data frame.}

\item{types}{Optional named character vector of column types; computed if not
supplied.}
}
\value{
A data frame with one row per date column (\code{column}, \code{n},
\code{n_missing}, \code{min}, \code{max}, \code{range_days}, \code{n_unique}, \code{max_gap_days}), or
\code{NULL} if there are no date columns.
}
\description{
For each \code{Date}/\code{POSIXct} column, reports the count, missingness, range, and
the largest gap between consecutive (sorted, unique) timestamps -- a quick way
to spot coverage holes in a time series.
}
\examples{
df <- data.frame(d = as.Date("2026-01-01") + c(0, 1, 2, 10))
analyze_dates(df)
}
