% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profiling.R
\name{data_quality_score}
\alias{data_quality_score}
\title{Data quality score}
\usage{
data_quality_score(
  df,
  missing = NULL,
  outlier_rate = NULL,
  weights = c(completeness = 0.4, uniqueness = 0.2, variability = 0.2, cleanliness = 0.2)
)
}
\arguments{
\item{df}{A data frame.}

\item{missing}{Optional result of \code{\link[=analyze_missing]{analyze_missing()}}; computed if \code{NULL}.}

\item{outlier_rate}{Optional fraction (0-1) of numeric cells flagged as
outliers; if supplied, a cleanliness component is included.}

\item{weights}{Optional named numeric vector of component weights. Missing
components are dropped and the rest renormalised.}
}
\value{
A list with \code{score} (0-100), \code{grade} (a letter), and \code{components}
(a named numeric vector of the component scores).
}
\description{
Rolls several signals into a single 0-100 score and a letter grade. The
components are completeness (share of non-missing cells), row uniqueness
(penalises duplicate rows), and column variability (penalises constant,
single-value columns). If an \code{outlier_rate} is supplied it adds a cleanliness
component. Components are averaged with the supplied \code{weights}.
}
\examples{
data_quality_score(iris)
}
