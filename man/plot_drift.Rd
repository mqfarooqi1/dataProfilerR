% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{plot_drift}
\alias{plot_drift}
\title{Bar chart of per-column drift}
\usage{
plot_drift(reference, current, bins = 10, psi_threshold = 0.2)
}
\arguments{
\item{reference, current}{Data frames to compare. Only columns present in both
are used.}

\item{bins}{Number of quantile bins for numeric PSI (default 10).}

\item{psi_threshold}{PSI at or above which a column is flagged as drifted
(default 0.2).}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the PSI of each column from \code{\link[=compare_datasets]{compare_datasets()}}, highlighting columns
that exceed the drift threshold.
}
\examples{
set.seed(1)
ref <- data.frame(x = rnorm(200), y = rnorm(200))
cur <- data.frame(x = rnorm(200, 1), y = rnorm(200))
plot_drift(ref, cur)
}
