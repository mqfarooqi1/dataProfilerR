% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile_data.R
\name{profile_data}
\alias{profile_data}
\title{Profile a data frame}
\usage{
profile_data(
  df,
  dataset_name = NULL,
  build_plots = TRUE,
  distributions = TRUE,
  normality = TRUE,
  outlier_method = "iqr",
  cor_method = c("pearson", "spearman"),
  group_by = NULL,
  verbose = FALSE
)
}
\arguments{
\item{df}{A data frame with at least one row and one column and unique,
non-empty column names.}

\item{dataset_name}{Optional label stored in the metadata; defaults to the
deparsed name of \code{df}.}

\item{build_plots}{Whether to build the \pkg{ggplot2} objects. Set \code{FALSE} to
skip plotting on very wide data. Default \code{TRUE}.}

\item{distributions}{Whether to build a per-column distribution plot (the
eager, heaviest part of plotting). Set \code{FALSE} on wide data and use
\code{\link[=plot_distribution]{plot_distribution()}} on demand. Ignored if \code{build_plots = FALSE}.
Default \code{TRUE}.}

\item{normality}{Whether to run normality tests. Default \code{TRUE}.}

\item{outlier_method}{Method passed to \code{\link[=outlier_summary]{outlier_summary()}}: \code{"iqr"},
\code{"zscore"} or \code{"robust"}. Default \code{"iqr"}.}

\item{cor_method}{Correlation methods: any of \code{"pearson"}, \code{"spearman"}.}

\item{group_by}{Optional name of a categorical column. If supplied, a grouped
comparison of the numeric columns is added to the diagnostics (see
\code{\link[=compare_groups]{compare_groups()}}).}

\item{verbose}{Print progress messages. Default \code{FALSE}.}
}
\value{
An object of class \code{data_profile}: a list with elements \code{metadata},
\code{statistics}, \code{diagnostics}, \code{plots} and \code{call}.
}
\description{
The package's single entry point. It runs type inference, missing-value
analysis, summary statistics, normality tests, outlier detection, correlation
analysis and a data-quality score, and (optionally) builds a set of
\pkg{ggplot2} visualisations. The result is a \code{data_profile} S3 object with
\code{print()}, \code{summary()} and \code{plot()} methods.
}
\examples{
p <- profile_data(iris)
p
summary(p)
\donttest{
plot(p, which = "correlation")
}
}
\seealso{
\code{\link[=print.data_profile]{print.data_profile()}}, \code{\link[=summary.data_profile]{summary.data_profile()}},
\code{\link[=plot.data_profile]{plot.data_profile()}}
}
