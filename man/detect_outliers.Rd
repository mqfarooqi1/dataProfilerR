% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{detect_outliers}
\alias{detect_outliers}
\title{Detect outliers in a numeric vector}
\usage{
detect_outliers(x, method = c("iqr", "zscore", "robust"), threshold = NULL)
}
\arguments{
\item{x}{A numeric vector.}

\item{method}{One of \code{"iqr"}, \code{"zscore"}, \code{"robust"}.}

\item{threshold}{Cutoff for \code{"zscore"}/\code{"robust"}; the IQR multiplier for
\code{"iqr"}. Defaults: 1.5 (iqr), 3 (zscore), 3.5 (robust).}
}
\value{
A list: \code{method}, \code{n} (non-missing count), \code{n_outliers}, \code{pct},
\code{is_outlier} (a logical vector aligned to \code{x}, \code{FALSE} for \code{NA}), and
\code{bounds} (lower/upper, where applicable).
}
\description{
Three standard rules:
\itemize{
\item \code{"iqr"}: outside \code{Q1 - k*IQR} / \code{Q3 + k*IQR} (Tukey's rule, \code{k = 1.5}).
\item \code{"zscore"}: absolute z-score above \code{threshold} (default 3).
\item \code{"robust"}: absolute modified z-score using the median and MAD above
\code{threshold} (default 3.5); resistant to the outliers it is detecting.
}
}
\examples{
detect_outliers(c(1, 2, 3, 4, 100), method = "iqr")
}
