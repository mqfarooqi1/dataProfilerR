% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_missing}
\alias{plot_missing}
\title{Missing-value heatmap}
\usage{
plot_missing(df, max_rows = 500)
}
\arguments{
\item{df}{A data frame.}

\item{max_rows}{Maximum rows to display (subsampled if exceeded). Default 500.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
A tile plot of where \code{NA}s fall: columns on the x-axis, rows on the y-axis,
shaded by whether each cell is missing. For wide/tall data the rows are
subsampled to \code{max_rows} so the plot stays legible.
}
\examples{
df <- data.frame(a = c(1, NA, 3), b = c(NA, "y", "z"))
plot_missing(df)
}
