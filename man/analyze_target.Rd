% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target.R
\name{analyze_target}
\alias{analyze_target}
\title{Rank features by their association with a target variable}
\usage{
analyze_target(data, target, types = NULL, max_levels = 50)
}
\arguments{
\item{data}{A data frame.}

\item{target}{Name of the target column (a single string).}

\item{types}{Optional named vector of column types (as from
\code{\link[=infer_column_types]{infer_column_types()}}); computed if not supplied.}

\item{max_levels}{Categorical variables with more distinct levels than this
are skipped (reported with \code{NA} strength). Default 50.}
}
\value{
A data frame, sorted by descending \code{strength}, with columns
\code{feature}, \code{type}, \code{method}, \code{strength} (0-1), \code{p_value} and \code{n} (the number
of complete observations used).
}
\description{
For supervised problems, \code{analyze_target()} measures how strongly each column
is associated with a chosen target column and returns a ranked table. The
measure adapts to the variable types so numeric and categorical predictors
are comparable on a common 0-1 scale:
}
\details{
\itemize{
\item numeric target and numeric feature - absolute Pearson correlation;
\item numeric with categorical (either role) - the correlation ratio (eta),
the square root of the ANOVA between-group variance proportion;
\item categorical target and categorical feature - Cramer's V.
}

This gives a quick, model-free view of which variables are most predictive
before any modelling.
}
\examples{
df <- data.frame(
  y = c(1, 2, 3, 4, 5, 6),
  x_num = c(1.1, 1.9, 3.2, 3.8, 5.1, 6.2),
  x_cat = factor(c("a", "a", "b", "b", "c", "c"))
)
analyze_target(df, "y")
}
\seealso{
\code{\link[=correlation_analysis]{correlation_analysis()}}, \code{\link[=categorical_association]{categorical_association()}}
}
