% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/groups.R
\name{compare_groups}
\alias{compare_groups}
\title{Compare numeric columns across groups}
\usage{
compare_groups(df, group, max_groups = 50)
}
\arguments{
\item{df}{A data frame.}

\item{group}{Name of the grouping column. Should be categorical/logical (or a
low-cardinality column); a warning is issued if it has many levels.}

\item{max_groups}{Maximum number of groups before erroring (guards against
accidentally grouping on a near-unique column). Default 50.}
}
\value{
A list with \code{group_sizes} (a data frame of \code{group}, \code{n}) and
\code{numeric_by_group} (a long data frame of \code{group}, \code{column}, \code{n},
\code{n_missing}, \code{mean}, \code{sd}, \code{median}), or \code{NULL} if there are no numeric
columns to compare.
}
\description{
Grouped profiling: split the data by a categorical column and summarise each
numeric column within each group (count, mean, sd, median, missingness). This
is the quickest way to see whether a metric differs by segment.
}
\examples{
compare_groups(iris, "Species")
}
