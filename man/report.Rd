% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{report}
\alias{report}
\title{Render a profile to a self-contained HTML report}
\usage{
report(
  x,
  output_file = "dataProfilerR_report.html",
  title = NULL,
  quiet = TRUE
)
}
\arguments{
\item{x}{A \code{data_profile} (built with \code{build_plots = TRUE}).}

\item{output_file}{Path to write. A bare file name lands in the working
directory. Default \code{"dataProfilerR_report.html"}.}

\item{title}{Report title. Defaults to the dataset name.}

\item{quiet}{Passed to \code{\link[rmarkdown:render]{rmarkdown::render()}}. Default \code{TRUE}.}
}
\value{
The path to the written file, invisibly.
}
\description{
Turns a \code{data_profile} into a standalone HTML file containing the metadata,
quality score, statistical tables and every figure. The report is built with
\pkg{rmarkdown}, so a working pandoc installation is required (R Markdown's
usual dependency); \code{report()} errors clearly if pandoc is unavailable.
}
\examples{
\donttest{
if (requireNamespace("rmarkdown", quietly = TRUE) &&
    rmarkdown::pandoc_available()) {
  p <- profile_data(iris)
  f <- report(p, file.path(tempdir(), "iris_report.html"))
}
}
}
