% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare_datasets}
\alias{compare_datasets}
\title{Detect distribution drift between two datasets}
\usage{
compare_datasets(reference, current, bins = 10, psi_threshold = 0.2)
}
\arguments{
\item{reference, current}{Data frames to compare. Only columns present in both
are used.}

\item{bins}{Number of quantile bins for numeric PSI (default 10).}

\item{psi_threshold}{PSI at or above which a column is flagged as drifted
(default 0.2).}
}
\value{
A data frame, sorted by descending \code{psi}, with columns \code{column},
\code{type}, \code{psi}, \code{stat} (KS statistic for numeric columns, \code{NA} otherwise),
\code{p_value} and \code{drifted} (logical).
}
\description{
Compares a \code{reference} data frame with a \code{current} one, column by column, to
flag features whose distribution has shifted - for example train versus test,
or an old versus a new batch of data. For each shared column it reports the
population stability index (PSI) and a distributional test:
}
\details{
\itemize{
\item numeric columns - PSI over reference quantile bins, plus a two-sample
Kolmogorov-Smirnov statistic and p-value;
\item categorical/logical columns - PSI over categories, plus a chi-squared
p-value.
}

PSI is a standard drift metric: values below 0.1 indicate little shift,
0.1-0.2 a moderate shift, and above 0.2 a substantial shift.
}
\examples{
set.seed(1)
ref <- data.frame(x = rnorm(200), g = sample(c("a", "b"), 200, TRUE))
cur <- data.frame(x = rnorm(200, mean = 1), g = sample(c("a", "b"), 200, TRUE))
compare_datasets(ref, cur)
}
\seealso{
\code{\link[=compare_groups]{compare_groups()}}, \code{\link[=profile_data]{profile_data()}}
}
